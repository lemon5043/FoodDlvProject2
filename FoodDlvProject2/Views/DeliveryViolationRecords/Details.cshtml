@model IEnumerable<FoodDlvProject2.Models.EFModels.DeliveryViolationRecord>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.OrderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Violation.ViolationContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Violation.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ViolationDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Violation.ViolationContent)
                </td>
                                <td>
                    @Html.DisplayFor(modelItem => item.Violation.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ViolationDate)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.OrderId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.OrderId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

